# personal-finance-agent
Your Personal Finance Agent is a smart web app that helps track and manage money like a robot helper. It uses AI "agents" to grab data, spot spending habits, and make budgets—runs in your browser via Streamlit.

## Run
1. pip install -r requirements.txt
2. streamlit run app.py

Open http://localhost:8501